# CRUX — bashrs 7.1.0

Status: rows written by three blind agy lanes (PMAT-255/ph12, group A: 3/3 PASS, lane-reduce agreed=true), read by the orchestrator. Every claim about another system is [X] — recalled, not measured.

## Orchestrator's reading

- **Provenance.** 58 of the 112 [V] citations point at the CHANGELOG bullet itself rather than at code, so those rows restate the claim they compare; 46 cite code, build or CI, and 8 docs or contracts (the delegate's resolution, repeated in Provenance below).
- **Dispositions.** Every comparison row keeps bashrs's choice. The two adopt rows in the lanes' dissent went to a blind three-lane quorum on 2026-09-11 and both were rejected 3-0. AST-based loop containment for SC2105: rejected, because the forms that diverged from shellcheck were fixed in PMAT-244 and the one remaining divergence belongs to SC2106, which bashrs does not implement (#303); the lanes noted the counter, that SC2105 is still on the corpus scoring exclusion list. cargo-release and release-plz: rejected, because the script handles the sparse-index race that broke v7.0.4 and the release protocol requires measured corpus numbers in the changelog that a generated changelog cannot produce; the counter is that one maintainer carries a custom script. Record: `docs/audits/quorum-decisions-v7.2.0.md`.
- **The same review's diff lanes** (group B: 3 lanes, all FAIL on different points, agreed=false; `docs/audits/quorum-PMAT-255-review.json`) were re-measured point by point before the release:

| point | claim | orchestrator | evidence |
|---|---|---|---|
| P1 | exec_flow ignores a method-call exec | refuted as an injection path | `m.exec()` lowers to `:` on 7.0.4 and 7.1.0 — no `eval` is emitted; the silent drop itself is #305 |
| P2 | array lowering loses empty elements; other consumers get "$name" | confirmed, fixed; follow-up filed | `array_len(["a", ""])` printed 1 and the join lost its separator; exact lowering now, two tests; `.len()` lowering to 'unknown' is #306 (pre-existing on 7.0.4) |
| P3 | a directive after `set -e` becomes file-level | confirmed, pre-existing; filed | identical on 7.0.4 and 7.1.0 (shellcheck reports SC2086 on the second line); #302 |
| P4 | DET005 masked when the value also reaches a DET002 sink | confirmed; filed | DET002 on line 2, nothing on the condition line 3; #304 |
| P5 | SC2105 misses break in a same-line function, subshell or after `# do` | function and comment cases refuted; subshell case filed | the function and comment forms now match shellcheck; `( break )` in a loop is shellcheck's SC2106, which bashrs does not implement — #303 |
| P6 | `${var:-$(date)}` changed its SC2046 verdict | confirmed regression, fixed | 7.0.4 SC2046, release candidate nothing, shellcheck SC2086; shell_words now scans the brace body; two tests |
| P7 | dogfood scripts can pass without measuring | no defect (2 lanes) | — |
| P8 | publish-from-tag trusts `--worktree`; dry run polls for an unpublished version | confirmed, fixed | refuses a worktree not at the tag's commit; a dependent dry run is skipped with a note instead of polling |
| P9 | the wait polls the web API, not the index cargo resolves against | confirmed (1 lane of 2), fixed | script and release.yml both read index.crates.io |
| P10 | stdout still carries output under --format json | no defect (1 lane) | the #301 test pins it |


## Method

Three agy 1.2.0 lanes ran with `--mode plan --sandbox`, the quorum schema and a 25 m timeout, each launched from its own empty working directory (PMAT-255, phase ph12, group A). All three got one identical prompt: the operator's CRUX instruction verbatim, the 12 bullets of CHANGELOG.md `[7.1.0]` verbatim, bullet labels B1–B12, and read-only access through view_file/grep_search to `release-diff.patch` (`git diff origin/main...PMAT-255-release` excluding `corpus_data.jsonl`, `docs/audits/**` and `Cargo.lock`; 47 files, 3974 lines) and to forjar's `scripts/publish-from-tag.sh` and `scripts/dogfood/`. There was no network and no repository checkout.

A claim about another system is **[X]**: the lane recalled it and did not measure it. A claim about bashrs is **[V]** and carries the lane's own citation. When that citation is `release-diff.patch:<line>`, the last column names the file whose hunk holds that patch line. The delegate computed that column from the patch's `diff --git` offsets, so it records provenance and verifies nothing. Rows are kept per lane and never merged. **This is a DRAFT: the orchestrator has re-verified no row.**

lane-reduce (`crux/lane-reduce.json`): `agreed:true`, `partial:false`, 36 `dedup` rows, `uncovered:[]`, `dissent:[]`, `coverage_source:"lanes"`. A second pass with `--brief` tokens `B1]`…`B12]` (on scratch copies) left `uncovered:[]` (an item counts as covered when any lane JSON holds its token); counted per lane, the rows cover L1 12/12, L2 12/12, L3 12/12 bullets. Of the 112 [V] citations, 58 resolve to the CHANGELOG.md hunk (the bullet text under review), 46 to code, build or CI, and 8 to docs or contracts; see Provenance.

| Lane | conversation | duration (s) | verdict | rows | dissent rows | adopt | reject |
|------|--------------|--------------|---------|------|--------------|-------|--------|
| L1 | `f0b5390b-cf07-4d4a-a574-fe9293156b89` | 217 | PASS | 36 | 1 | 1 | 36 |
| L2 | `dc7ede87-235d-451f-a4ef-42e7023d24d8` | 215 | PASS | 36 | 1 | 1 | 36 |
| L3 | `215174d3-5dcc-46d0-91c7-e25b39e6796c` | 195 | PASS | 36 | 2 | 0 | 36 |

## Findings

One row per (bullet, system, lane), sorted by bullet, then lane, in the order each lane wrote them.

| # | Bullet | Lane | System | How they do it [X] | bashrs today [V] | Delta | Disposition | Lane cite | Cite resolves to |
|---|--------|------|--------|--------------------|------------------|-------|-------------|-----------|------------------|
| 1 | B1 | L1 | Batsh | compiles array literals to native bash arrays (`arr=(...)`) [X] | operates on the elements of a local array literal via flattened scalars [V release-diff.patch:713] | Batsh assumes bash array support, whereas bashrs flattens to scalar variables to retain POSIX sh compatibility | reject(bashrs targets POSIX sh which lacks arrays) | `release-diff.patch:713` | rash/src/ir/expr_calls.rs |
| 2 | B1 | L1 | Amber | compiles source arrays directly into bash arrays [X] | operates on the elements of a local array literal via flattened scalars [V release-diff.patch:713] | Amber targets bash only, bashrs targets strictly POSIX sh | reject(bashrs targets POSIX sh without native arrays) | `release-diff.patch:713` | rash/src/ir/expr_calls.rs |
| 3 | B1 | L1 | Oil/YSH | implements a distinct shell dialect with first-class arrays [X] | operates on the elements of a local array literal via flattened scalars [V release-diff.patch:713] | YSH is a runtime engine, bashrs is a source-to-shell compiler targeting standard POSIX | reject(bashrs must compile to POSIX sh) | `release-diff.patch:713` | rash/src/ir/expr_calls.rs |
| 4 | B1 | L2 | POSIX shell quoting rules | Arrays do not exist in POSIX shell, elements are managed via positional parameters [X] | Expands array literals to items_0, items_1 for array_join and array_len [V release-diff.patch:50] | bashrs implements array simulation on top of POSIX | reject(POSIX shell compatibility requires this emulation) | `release-diff.patch:50` | CHANGELOG.md |
| 5 | B1 | L2 | Amber | Generates space-separated strings or native arrays based on target [X] | Uses items_0, items_1 and passes elements to helpers [V release-diff.patch:50] | Amber has complex type lowering, bashrs uses simple indexed variables | reject(indexed variable simulation is sufficient for dash) | `release-diff.patch:50` | CHANGELOG.md |
| 6 | B1 | L2 | Batsh | Compiles arrays into bash native arrays [X] | Lowers arrays to indexed variables items_0 etc. for POSIX compatibility [V release-diff.patch:50] | Batsh relies on bash arrays, bashrs avoids them | reject(bashrs targets POSIX sh) | `release-diff.patch:50` | CHANGELOG.md |
| 7 | B1 | L3 | Batsh | compiles array literals to native Bash arrays when not restricted to POSIX sh [X] | expands first argument to the element list the runtime helpers read [V release-diff.patch:50] | bashrs dynamically expands elements for its POSIX sh helpers rather than using native arrays | reject(bashrs targets POSIX sh which lacks arrays) | `release-diff.patch:50` | CHANGELOG.md |
| 8 | B1 | L3 | Amber | compiles to bash and leverages native bash array syntax for safety [X] | expands first argument to the element list the runtime helpers read [V release-diff.patch:50] | bashrs avoids native arrays to maintain wider POSIX shell compatibility | reject(bashrs targets POSIX sh which lacks arrays) | `release-diff.patch:50` | CHANGELOG.md |
| 9 | B1 | L3 | Oil/YSH | provides first-class array types built into its own runtime [X] | expands first argument to the element list the runtime helpers read [V release-diff.patch:50] | bashrs transpiles to standard shell rather than requiring a custom shell binary | reject(bashrs targets POSIX sh which lacks arrays) | `release-diff.patch:50` | CHANGELOG.md |
| 10 | B2 | L1 | POSIX shell quoting rules | single quotes prevent all interpolation, rendering `$()` and backticks inert [X] | a standalone literal is emitted single-quoted, where `$()` is inert [V release-diff.patch:2955] | bashrs now accurately reflects POSIX rules by not refusing these strings pre-emission | reject(bashrs is now perfectly aligned with POSIX) | `release-diff.patch:2955` | rash/src/validation/pipeline_tests.rs |
| 11 | B2 | L1 | mvdan/sh's printer | uses single quotes for strings containing metacharacters to safely print them without expansion [X] | a standalone literal is emitted single-quoted, where `$()` is inert [V release-diff.patch:2955] | bashrs emits literals identically to mvdan/sh's safe output | reject(bashrs is aligned with standard printing practices) | `release-diff.patch:2955` | rash/src/validation/pipeline_tests.rs |
| 12 | B2 | L1 | shellcheck SC2016 | warns about `$()` inside single quotes as developers may expect them to expand [X] | a standalone literal is emitted single-quoted, where `$()` is inert [V release-diff.patch:2955] | bashrs intentionally single-quotes these as an emitter, it is not a developer writing a bug | reject(bashrs treats them as inert data purposefully) | `release-diff.patch:2955` | rash/src/validation/pipeline_tests.rs |
| 13 | B2 | L2 | shellcheck SC2006/SC2016 | SC2016 warns about backticks inside single quotes, SC2006 reports backticks universally [X] | Allows inert backticks/substitutions when emitted single-quoted, rejecting only live text in exec/capture [V release-diff.patch:51] | bashrs distinguishes inert emitted text from raw embedded text | reject(bashrs safely allows inert literals in single quotes) | `release-diff.patch:51` | CHANGELOG.md |
| 14 | B2 | L2 | mvdan/sh | Prints AST verbatim, escaping or quoting based on parsed context [X] | Validates literals against command substitution before emission [V release-diff.patch:51] | bashrs acts as a security linter during transpilation | reject(bashrs enforces security limits over just printing AST) | `release-diff.patch:51` | CHANGELOG.md |
| 15 | B2 | L2 | Oil/YSH | Provides new J8 string types that refuse implicit expansion [X] | Statically checks if live shell text reaches eval [V release-diff.patch:51] | Oil changes the language, bashrs analyzes standard sh strings | reject(bashrs checks standard shell strings statically) | `release-diff.patch:51` | CHANGELOG.md |
| 16 | B2 | L3 | POSIX shell | treats all characters including backticks as inert inside single quotes [X] | allows string literal holding a backtick or substitution when emitted single-quoted [V release-diff.patch:51] | bashrs now fully aligns its emitter validation with POSIX single-quote rules | reject(correct alignment with POSIX rules) | `release-diff.patch:51` | CHANGELOG.md |
| 17 | B2 | L3 | shellcheck | issues SC2016 when backticks are in single quotes to warn against unintended literalness [X] | allows string literal holding a backtick or substitution when emitted single-quoted [V release-diff.patch:51] | bashrs transpiler emits single quotes intentionally for safety, which shellcheck would warn on as human error | reject(transpiler output is machine-generated, SC2016 applies to humans) | `release-diff.patch:51` | CHANGELOG.md |
| 18 | B2 | L3 | shellharden | aggressively quotes variables and uses single quotes to guarantee bytes are inert [X] | allows string literal holding a backtick or substitution when emitted single-quoted [V release-diff.patch:51] | both tools rely on single quotes to safely emit arbitrary data | reject(correct alignment with POSIX rules) | `release-diff.patch:51` | CHANGELOG.md |
| 19 | B3 | L1 | shellcheck | uses AST containment to determine if a break is lexically inside a loop [X] | processes loop-start keywords, done and break/continue in the byte order they occur on a physical line [V release-diff.patch:1521] | shellcheck parses the syntax tree, bashrs tracks inline byte-order state to avoid false positives on one-liners | reject(bashrs byte-order tracking correctly resolves the false positive) | `release-diff.patch:1521` | rash/src/linter/rules/sc2105.rs |
| 20 | B3 | L1 | mvdan/sh | constructs a full AST where loop containment is absolute [X] | processes loop-start keywords, done and break/continue in the byte order they occur on a physical line [V release-diff.patch:1521] | bashrs achieves the same correctness for inline loops without requiring full AST traversal | reject(bashrs achieves the same result efficiently) | `release-diff.patch:1521` | rash/src/linter/rules/sc2105.rs |
| 21 | B3 | L1 | tree-sitter-bash | uses a Concrete Syntax Tree where a break is a child of a loop node [X] | processes loop-start keywords, done and break/continue in the byte order they occur on a physical line [V release-diff.patch:1521] | bashrs avoids CST overhead by doing a lexical byte-order scan | reject(lexical byte order tracking is sufficient for this check) | `release-diff.patch:1521` | rash/src/linter/rules/sc2105.rs |
| 22 | B3 | L2 | shellcheck | Analyzes AST depth to determine if break is inside a loop, regardless of physical lines [X] | Follows loop structure instead of counting keywords per physical line [V release-diff.patch:52] | bashrs now matches shellcheck's AST-based loop context tracking | reject(bashrs already matches shellcheck) | `release-diff.patch:52` | CHANGELOG.md |
| 23 | B3 | L2 | tree-sitter-bash | Concrete syntax tree intrinsically scopes break within loop bodies [X] | Processes loop keywords in byte order to correctly scope one-line loops [V release-diff.patch:384] | bashrs uses sequential parsing matching tree-sitter scoping | reject(bashrs now handles one-line loops correctly) | `release-diff.patch:384` | contracts/linter-lexer-context-v1.yaml |
| 24 | B3 | L2 | bash | Evaluates break at runtime and errors if outside a loop [X] | Statically reports a bare break outside any loop [V release-diff.patch:52] | bashrs finds the error statically | reject(static detection is safer than runtime failure) | `release-diff.patch:52` | CHANGELOG.md |
| 25 | B3 | L3 | shellcheck | analyzes the AST to accurately detect break statements outside of loop bodies [X] | follows the loop structure, reporting break outside any loop [V release-diff.patch:52] | bashrs has migrated from naive per-line heuristic to AST structural analysis like shellcheck | reject(bashrs has adopted the correct AST approach) | `release-diff.patch:52` | CHANGELOG.md |
| 26 | B3 | L3 | bash | throws a runtime error if a break statement executes outside of a loop [X] | follows the loop structure, reporting break outside any loop [V release-diff.patch:52] | bashrs statically lints what bash would refuse at runtime | reject(bashrs has adopted the correct AST approach) | `release-diff.patch:52` | CHANGELOG.md |
| 27 | B3 | L3 | mvdan/sh | parses loop boundaries accurately to allow linters to query valid break contexts [X] | follows the loop structure, reporting break outside any loop [V release-diff.patch:52] | bashrs achieves the same structural accuracy as mvdan/sh | reject(bashrs has adopted the correct AST approach) | `release-diff.patch:52` | CHANGELOG.md |
| 28 | B4 | L1 | shellcheck | shellcheck -f json outputs exclusively JSON to stdout and logs to stderr [X] | logs go to stderr; stdout carries only the requested output [V release-diff.patch:700] | bashrs now strictly aligns with the standard JSON CLI output convention | reject(bashrs is strictly aligned) | `release-diff.patch:700` | rash/src/cli/commands.rs |
| 29 | B4 | L1 | hadolint | -f json outputs a JSON array strictly to stdout [X] | logs go to stderr; stdout carries only the requested output [V release-diff.patch:700] | bashrs fixed its tracing output leaking into stdout | reject(bashrs is strictly aligned) | `release-diff.patch:700` | rash/src/cli/commands.rs |
| 30 | B4 | L1 | trivy | --format json redirects informational logs to stderr [X] | logs go to stderr; stdout carries only the requested output [V release-diff.patch:700] | bashrs behavior is identical | reject(bashrs is strictly aligned) | `release-diff.patch:700` | rash/src/cli/commands.rs |
| 31 | B4 | L2 | shellcheck | Outputs pure JSON to stdout, sending errors and info to stderr [X] | Writes exactly one JSON document to stdout, moving tracing to stderr [V release-diff.patch:53] | bashrs conforms to shellcheck's output streams | reject(bashrs now correctly separates data and tracing) | `release-diff.patch:53` | CHANGELOG.md |
| 32 | B4 | L2 | hadolint | Emits a single JSON array to stdout without interleaved text [X] | Prints exactly one JSON document on stdout [V release-diff.patch:53] | No difference, standard JSON CLI behavior | reject(bashrs output is standard) | `release-diff.patch:53` | CHANGELOG.md |
| 33 | B4 | L2 | semgrep | Uses --json for pure JSON output to stdout [X] | Uses --format json to achieve pure JSON on stdout [V release-diff.patch:53] | Different flag names, same standard behavior | reject(matches ecosystem standards) | `release-diff.patch:53` | CHANGELOG.md |
| 34 | B4 | L3 | shellcheck | writes pure JSON to stdout and diagnostics to stderr [X] | writes tracing INFO to stderr to ensure exactly one JSON document on stdout [V release-diff.patch:53] | bashrs aligns with standard CLI practices for machine-readable output | reject(strict stdout/stderr separation is standard) | `release-diff.patch:53` | CHANGELOG.md |
| 35 | B4 | L3 | hadolint | ensures stdout only contains valid JSON when requested [X] | writes tracing INFO to stderr to ensure exactly one JSON document on stdout [V release-diff.patch:53] | bashrs now matches hadolint's strict JSON output | reject(strict stdout/stderr separation is standard) | `release-diff.patch:53` | CHANGELOG.md |
| 36 | B4 | L3 | checkov | outputs parsed JSON directly to stdout for pipeline consumption [X] | writes tracing INFO to stderr to ensure exactly one JSON document on stdout [V release-diff.patch:53] | bashrs now guarantees pipeline safety for JSON format | reject(strict stdout/stderr separation is standard) | `release-diff.patch:53` | CHANGELOG.md |
| 37 | B5 | L1 | cargo's own workspace behaviour | requires tests to use unwind for catch_unwind, breaking --workspace tests if panic=abort is set [X] | no panic = "abort" here. Built in one invocation [V release-diff.patch:94] | bashrs removed the abort profile to allow workspace-wide testing in one pass | reject(bashrs correctly aligned with cargo's expectations) | `release-diff.patch:94` | Cargo.toml |
| 38 | B5 | L1 | cargo-release | verifies the workspace by building and testing all targets in one invocation [X] | no panic = "abort" here. Built in one invocation [V release-diff.patch:94] | bashrs now natively supports this workflow | reject(bashrs is aligned) | `release-diff.patch:94` | Cargo.toml |
| 39 | B5 | L1 | release-plz | builds and tests all crates in a workspace together [X] | no panic = "abort" here. Built in one invocation [V release-diff.patch:94] | bashrs no longer blocks this with dev-profile panics | reject(bashrs is aligned) | `release-diff.patch:94` | Cargo.toml |
| 40 | B5 | L2 | cargo's own workspace behaviour | Unifies profiles across workspace, failing if one crate needs unwind and another uses abort [X] | Removes panic=abort from dev profile so probar test targets build correctly [V release-diff.patch:93] | bashrs accommodates cargo's workspace unification limitations | reject(bashrs correctly resolves cargo workspace constraints) | `release-diff.patch:93` | Cargo.toml |
| 41 | B5 | L2 | cargo-release | Verifies workspace builds before publishing, catching profile conflicts early [X] | Ensures every target builds in one invocation [V release-diff.patch:54] | Same build assurance strategy | reject(bashrs fixed the underlying build issue) | `release-diff.patch:54` | CHANGELOG.md |
| 42 | B5 | L2 | release-plz | Automates workspace builds relying on cargo's dependency resolution [X] | Uses cargo build --workspace --all-targets [V release-diff.patch:94] | bashrs ensures manual builds succeed like release-plz | reject(bashrs manual build is now correct) | `release-diff.patch:94` | Cargo.toml |
| 43 | B5 | L3 | cargo workspace | unifies profile settings across workspace members causing test binaries to inherit abort if not overridden [X] | dev profile removed panic abort so bashrs-wasm probar test target compiles [V release-diff.patch:94] | bashrs adapts to cargo's workspace profile unification by dropping abort in dev | reject(necessary adaptation to cargo workspace mechanics) | `release-diff.patch:94` | Cargo.toml |
| 44 | B5 | L3 | cargo-release | relies on the workspace's Cargo.toml definitions and tests in dev profile before releasing [X] | dev profile removed panic abort so bashrs-wasm probar test target compiles [V release-diff.patch:94] | bashrs ensures dev profile is testable for tooling | reject(necessary adaptation to cargo workspace mechanics) | `release-diff.patch:94` | Cargo.toml |
| 45 | B5 | L3 | release-plz | executes cargo test on the workspace which requires compiling all targets including tests [X] | dev profile removed panic abort so bashrs-wasm probar test target compiles [V release-diff.patch:94] | bashrs allows workspace-wide test verification to pass | reject(necessary adaptation to cargo workspace mechanics) | `release-diff.patch:94` | Cargo.toml |
| 46 | B6 | L1 | forjar's scripts/publish-from-tag.sh | checks out a detached worktree of the tag and loops cargo publish with poll_index backoff [V forjar:/home/noah/src/forjar/scripts/publish-from-tag.sh:114] | publish-from-tag.sh publishes from a clean detached worktree of the tag, bashrs-oracle first, waiting for the index before bashrs [V release-diff.patch:55] | bashrs exactly duplicates the forjar phase 5 detached worktree logic | reject(bashrs successfully adopted forjar's phase 5 release process) | `release-diff.patch:55` | CHANGELOG.md |
| 47 | B6 | L1 | cargo-release | publishes crates in topological order and waits for the index [X] | publish-from-tag.sh publishes from a clean detached worktree of the tag, bashrs-oracle first, waiting for the index before bashrs [V release-diff.patch:55] | bashrs implemented equivalent topo-sort and polling explicitly | reject(bashrs is aligned) | `release-diff.patch:55` | CHANGELOG.md |
| 48 | B6 | L1 | release-plz | publishes and polls crates.io until the crate is available before publishing dependents [X] | release.yml now waits for it before packaging bashrs [V release-diff.patch:9] | bashrs GitHub Actions explicitly wait on the crates.io API | reject(bashrs handles the index delay robustly) | `release-diff.patch:9` | .github/workflows/release.yml |
| 49 | B6 | L2 | forjar's scripts/publish-from-tag.sh | Publishes from a detached worktree, waits for index with bounded poll before downstream crates [V forjar:scripts/publish-from-tag.sh:2] | Publishes from detached worktree, bashrs-oracle first, polling index before bashrs [V release-diff.patch:55] | bashrs adopted forjar's publishing strategy | reject(bashrs already adopted forjar's approach) | `release-diff.patch:55` | CHANGELOG.md |
| 50 | B6 | L2 | cargo-release | Automatically checks index between dependent workspace crates to avoid races [X] | Uses a bounded poll (10x30s) in release.yml and scripts [V release-diff.patch:15] | bashrs implements manual polling rather than using cargo-release | reject(bashrs scripted polling is sufficient) | `release-diff.patch:15` | .github/workflows/release.yml |
| 51 | B6 | L2 | release-plz | Handles topological publishing and waits natively [X] | Uses script-based topological sorting and polling [V release-diff.patch:117] | bashrs automates via scripts instead of Rust tools | reject(bashrs already implements topological publishing) | `release-diff.patch:117` | Makefile |
| 52 | B6 | L3 | forjar | publishes from a detached worktree and polls crates.io [V forjar:scripts/publish-from-tag.sh:226] | release.yml waits for bashrs-oracle before packaging bashrs via bounded poll [V release-diff.patch:10] | bashrs successfully mirrors forjar's race-free publish strategy | reject(forjar script is the proven secure model) | `release-diff.patch:10` | .github/workflows/release.yml |
| 53 | B6 | L3 | release-plz | automates topological publishing and handles index eventual consistency natively [X] | release.yml waits for bashrs-oracle before packaging bashrs via bounded poll [V release-diff.patch:10] | bashrs uses a manual shell script while release-plz automates this fully | reject(bashrs's script provides superior isolation via detached worktrees) | `release-diff.patch:10` | .github/workflows/release.yml |
| 54 | B6 | L3 | cargo workspace | requires manual topology resolution or external tools to avoid index races [X] | release.yml waits for bashrs-oracle before packaging bashrs via bounded poll [V release-diff.patch:10] | bashrs fills the gap in cargo's native workspace publishing capabilities | reject(forjar script is the proven secure model) | `release-diff.patch:10` | .github/workflows/release.yml |
| 55 | B7 | L1 | shellcheck | cannot parse non-SC codes in disable directives and abandons the whole file [X] | reports BASHRS001 for non-SC codes instead of silently honouring them [V release-diff.patch:2176] | bashrs explicitly rejects hijacking shellcheck's directive for its own codes to prevent catastrophic failures in shellcheck | reject(bashrs correctly preserves shellcheck compatibility) | `release-diff.patch:2176` | rash/src/linter/suppression.rs |
| 56 | B7 | L1 | semgrep bash rules | uses its own # nosemgrep syntax for inline exclusions [X] | reports BASHRS001 for non-SC codes instead of silently honouring them [V release-diff.patch:2176] | bashrs adopted # bashrs disable-line= mirroring semgrep's isolated namespace | reject(bashrs correctly isolates its directives) | `release-diff.patch:2176` | rash/src/linter/suppression.rs |
| 57 | B7 | L1 | shellharden | ignores shellcheck directives entirely [X] | reports BASHRS001 for non-SC codes instead of silently honouring them [V release-diff.patch:2176] | bashrs warns when users mistakenly overload the shellcheck directive | reject(bashrs guides users to the correct namespace) | `release-diff.patch:2176` | rash/src/linter/suppression.rs |
| 58 | B7 | L2 | shellcheck | Abandons checking the rest of the file if it encounters a non-SC code in a disable directive [X] | Reports non-SC codes in shellcheck directives as BASHRS001 and ignores them [V release-diff.patch:346] | bashrs protects shellcheck from crashing on bashrs codes | reject(strict enforcement prevents silent shellcheck failures) | `release-diff.patch:346` | book/src/reference/rules.md |
| 59 | B7 | L2 | semgrep bash rules | Uses its own nosemgrep directive to avoid conflicts [X] | Requires bashrs disable-line= or disable-file= for its own codes [V release-diff.patch:366] | bashrs uses a dedicated namespace like semgrep | reject(namespace isolation is the industry standard) | `release-diff.patch:366` | book/src/reference/rules.md |
| 60 | B7 | L2 | shfmt | Ignores shellcheck directives for formatting [X] | Parses them to filter out non-SC codes and preserve shellcheck [V release-diff.patch:59] | bashrs actively interoperates with shellcheck | reject(interop is necessary for dual-linting) | `release-diff.patch:59` | CHANGELOG.md |
| 61 | B7 | L3 | shellcheck | ignores unknown directive codes and only honors SC-prefixed rules [X] | reports bashrs codes in shellcheck disable= as BASHRS001 and does not honour them [V release-diff.patch:59] | bashrs stops hijacking shellcheck's directive space and defines its own | reject(tool-specific namespaces prevent conflicts) | `release-diff.patch:59` | CHANGELOG.md |
| 62 | B7 | L3 | semgrep | uses its own nosem or semgrep:disable directives exclusively [X] | reports bashrs codes in shellcheck disable= as BASHRS001 and does not honour them [V release-diff.patch:59] | bashrs aligns with semgrep by requiring its own bashrs disable-line= directive | reject(tool-specific namespaces prevent conflicts) | `release-diff.patch:59` | CHANGELOG.md |
| 63 | B7 | L3 | shellharden | does not parse shellcheck disable directives [X] | reports bashrs codes in shellcheck disable= as BASHRS001 and does not honour them [V release-diff.patch:59] | bashrs strictly separates its suppression from other tools | reject(tool-specific namespaces prevent conflicts) | `release-diff.patch:59` | CHANGELOG.md |
| 64 | B8 | L1 | shellcheck's security checks | has no determinism rules and does not warn on elapsed time [X] | DET002 does NOT flag a duration - the arithmetic difference of two timestamp captures [V release-diff.patch:968] | bashrs's determinism engine explicitly exempts durations to avoid false positives | reject(bashrs's exemption is correctly tuned) | `release-diff.patch:968` | rash/src/linter/rules/det005.rs |
| 65 | B8 | L1 | semgrep bash rules | if checking for non-deterministic date functions, requires custom rule logic to exempt subtraction [X] | DET002 does NOT flag a duration - the arithmetic difference of two timestamp captures [V release-diff.patch:968] | bashrs builds this logic into the native rule | reject(bashrs native rule is robust) | `release-diff.patch:968` | rash/src/linter/rules/det005.rs |
| 66 | B8 | L1 | hadolint | no specific rule for bash execution durations, focuses on container caching [X] | DET002 does NOT flag a duration - the arithmetic difference of two timestamp captures [V release-diff.patch:968] | bashrs focuses on shell script determinism and correctly exempts elapsed time | reject(bashrs logic holds) | `release-diff.patch:968` | rash/src/linter/rules/det005.rs |
| 67 | B8 | L2 | shellcheck | Does not check for reproducibility, only shell syntax and common errors [X] | DET002 no longer reports elapsed time durations, only timestamps reaching artifacts [V release-diff.patch:60] | bashrs implements specialized determinism checks | reject(bashrs rules are domain-specific) | `release-diff.patch:60` | CHANGELOG.md |
| 68 | B8 | L2 | GNU make | Uses file modification times for dependencies, not wall-clock durations [X] | Allows durations since they do not affect artifact reproducibility [V release-diff.patch:213] | bashrs distinguishes durations from timestamps | reject(measuring elapsed time is not a determinism defect) | `release-diff.patch:213` | book/src/linting/determinism.md |
| 69 | B8 | L2 | bash | Provides SECONDS for durations, avoiding date +%s calls entirely [X] | Permits date +%s subtraction for elapsed time [V release-diff.patch:60] | bashrs accepts standard POSIX date math | reject(POSIX math is required for dash/sh) | `release-diff.patch:60` | CHANGELOG.md |
| 70 | B8 | L3 | shellcheck | does not track determinism or time flow [X] | exempts duration measurement since it reaches no artifact [V release-diff.patch:60] | bashrs implements advanced data-flow tracking for determinism that shellcheck lacks | reject(flow-based exemption is highly accurate) | `release-diff.patch:60` | CHANGELOG.md |
| 71 | B8 | L3 | semgrep | can flag sensitive APIs but requires complex taint rules to exempt specific safe data flows [X] | exempts duration measurement since it reaches no artifact [V release-diff.patch:60] | bashrs has native understanding of safe duration flows | reject(flow-based exemption is highly accurate) | `release-diff.patch:60` | CHANGELOG.md |
| 72 | B8 | L3 | hadolint | warns on wget/curl without versions but does not track time variables [X] | exempts duration measurement since it reaches no artifact [V release-diff.patch:60] | bashrs targets strict determinism specifically for artifacts | reject(flow-based exemption is highly accurate) | `release-diff.patch:60` | CHANGELOG.md |
| 73 | B9 | L1 | shellcheck | SC2046 uses the main AST to find unquoted command substitutions [X] | shell_words::Expansion now covers command-substitution [V release-diff.patch:1213] | bashrs previously used a private scanner, now uses the central shell_words lexer which is closer to an AST approach | reject(bashrs centralizes tokenization efficiently) | `release-diff.patch:1213` | rash/src/linter/rules/sc2046.rs |
| 74 | B9 | L1 | mvdan/sh | unified AST exposes CmdSubst nodes natively for any rule to inspect [X] | shell_words::Expansion now covers command-substitution [V release-diff.patch:1213] | bashrs exposes this via shell_words instead of an AST | reject(bashrs's lexer extension avoids full AST overhead) | `release-diff.patch:1213` | rash/src/linter/rules/sc2046.rs |
| 75 | B9 | L1 | tree-sitter-bash | command_substitution is a core AST node [X] | shell_words::Expansion now covers command-substitution [V release-diff.patch:1213] | bashrs's centralized lexer tracks it natively like an AST node | reject(bashrs is functionally aligned) | `release-diff.patch:1213` | rash/src/linter/rules/sc2046.rs |
| 76 | B9 | L2 | shellcheck | Uses its own AST to detect unquoted command substitutions [X] | SC2046 rule consumes command substitutions from shell_words instead of a private scanner [V release-diff.patch:61] | bashrs unified its expansion scanning | reject(bashrs internal refactor improves consistency) | `release-diff.patch:61` | CHANGELOG.md |
| 77 | B9 | L2 | mvdan/sh | Parses $( ) into CmdSubst nodes automatically [X] | shell_words::Expansion now covers $( ), nested $( $( ) ), and backticks [V release-diff.patch:414] | bashrs matches standard AST modeling of command substitutions | reject(bashrs now handles command substitutions robustly) | `release-diff.patch:414` | contracts/linter-lexer-context-v1.yaml |
| 78 | B9 | L2 | tree-sitter-bash | Creates command_substitution nodes directly in the tree [X] | shell_words provides the same structural understanding to SC2046 [V release-diff.patch:415] | bashrs provides similar structural information to its rules | reject(bashrs refactor achieves tree-like accuracy) | `release-diff.patch:415` | contracts/linter-lexer-context-v1.yaml |
| 79 | B9 | L3 | shellcheck | uses its central AST to identify word splitting contexts for command substitutions [X] | reads command substitutions from shell_words, removing its private scanner [V release-diff.patch:61] | bashrs moves to a central word scanner akin to shellcheck's AST approach | reject(architectural cleanup converging with AST best practices) | `release-diff.patch:61` | CHANGELOG.md |
| 80 | B9 | L3 | mvdan/sh | exposes word boundaries natively in the AST parser [X] | reads command substitutions from shell_words, removing its private scanner [V release-diff.patch:61] | bashrs consolidates word parsing into shell_words to avoid duplicating scanner logic | reject(architectural cleanup converging with AST best practices) | `release-diff.patch:61` | CHANGELOG.md |
| 81 | B9 | L3 | tree-sitter-bash | delineates word nodes directly in the syntax tree [X] | reads command substitutions from shell_words, removing its private scanner [V release-diff.patch:61] | bashrs mimics central structural parsing for word boundaries | reject(architectural cleanup converging with AST best practices) | `release-diff.patch:61` | CHANGELOG.md |
| 82 | B10 | L1 | forjar's scripts/dogfood | uses GATE D PASS\|FAIL in shell scripts for hermetic evaluation [V forjar:/home/noah/src/forjar/scripts/dogfood/docs.sh:4] | make dogfood runs mechanical gates ... Each prints one GATE <L> PASS\|FAIL line [V release-diff.patch:150] | bashrs implemented exactly the same mechanical gate standard and nomenclature | reject(bashrs successfully adopted forjar's dogfood standard) | `release-diff.patch:150` | Makefile |
| 83 | B10 | L1 | pre-commit | runs hooks via YAML configuration and orchestrates failures centrally [X] | make dogfood runs mechanical gates ... Each prints one GATE <L> PASS\|FAIL line [V release-diff.patch:150] | bashrs uses explicit bash scripts rather than YAML orchestration | reject(bashrs's explicit scripts are simpler and easily testable) | `release-diff.patch:150` | Makefile |
| 84 | B10 | L1 | shellcheck's own CI | uses a makefile and GitHub Actions to run tests and linters [X] | make dogfood runs mechanical gates ... Each prints one GATE <L> PASS\|FAIL line [V release-diff.patch:150] | bashrs consolidates validation into strict mechanical gates | reject(bashrs gates provide high accountability) | `release-diff.patch:150` | Makefile |
| 85 | B10 | L2 | forjar's scripts/dogfood | Uses hermetic gates (D, G, K) that print GATE PASS\|FAIL and stop the run on the first red gate [V forjar:scripts/dogfood/docs.sh:2] | Runs mechanical gates D, G, K in make dogfood exactly like forjar [V release-diff.patch:62] | bashrs adopted forjar's hermetic dogfood gates | reject(bashrs adopted forjar's gate system) | `release-diff.patch:62` | CHANGELOG.md |
| 86 | B10 | L2 | pre-commit | Runs isolated checks sequentially, failing the commit if any check fails [X] | Runs hermetic gates and stops the run on the first red gate without tolerating failures [V release-diff.patch:150] | bashrs uses make/scripts instead of pre-commit | reject(scripted gates provide equivalent hermeticity) | `release-diff.patch:150` | Makefile |
| 87 | B10 | L2 | shellcheck's own CI | Runs self-linting as part of CI pipelines [X] | Renamed self-linting to dogfood-selflint, making dogfood strictly for hermetic gates [V release-diff.patch:169] | bashrs separates reporting checks from hard gates | reject(distinction between gates and self-linting is superior) | `release-diff.patch:169` | Makefile |
| 88 | B10 | L3 | forjar | enforces hermetic gates that declare explicit mutations to prove they are not vacuous [X] | runs mechanical gates D, G, K, each carrying a mutation to turn it red [V release-diff.patch:62] | bashrs has fully adopted forjar's extreme falsifiability standard | reject(the mutation requirement proves the gate is not vacuous) | `release-diff.patch:62` | CHANGELOG.md |
| 89 | B10 | L3 | pre-commit | runs isolated checks but relies on developers to write separate tests for hook validity [X] | runs mechanical gates D, G, K, each carrying a mutation to turn it red [V release-diff.patch:62] | bashrs embeds falsifiability directly into the CI gate script itself | reject(the mutation requirement proves the gate is not vacuous) | `release-diff.patch:62` | CHANGELOG.md |
| 90 | B10 | L3 | shellcheck | uses standard Haskell unit and property tests in CI without explicit gate mutation lines [X] | runs mechanical gates D, G, K, each carrying a mutation to turn it red [V release-diff.patch:62] | bashrs's gates carry their own self-falsification | reject(the mutation requirement proves the gate is not vacuous) | `release-diff.patch:62` | CHANGELOG.md |
| 91 | B11 | L1 | shellcheck's security checks | does not have a rule for time-dependent control flow [X] | A wall-clock value in a branch condition is reported at warning [V release-diff.patch:1005] | bashrs implements DET005 to catch reproducibility issues in builds | reject(bashrs provides valuable reproducibility checks beyond standard linting) | `release-diff.patch:1005` | rash/src/linter/rules/det005.rs |
| 92 | B11 | L1 | semgrep bash rules | custom rules can flag date inside if statements [X] | A wall-clock value in a branch condition is reported at warning [V release-diff.patch:1005] | bashrs builds this in natively as DET005 for zero-config use | reject(bashrs native rule is faster and standardized) | `release-diff.patch:1005` | rash/src/linter/rules/det005.rs |
| 93 | B11 | L1 | shellharden | focuses purely on quoting variables, ignoring semantic control flow [X] | A wall-clock value in a branch condition is reported at warning [V release-diff.patch:1005] | bashrs checks logic and time non-determinism, expanding the definition of strictness | reject(bashrs logic check is necessary for build determinism) | `release-diff.patch:1005` | rash/src/linter/rules/det005.rs |
| 94 | B11 | L2 | shellcheck | Does not check for time-dependent control flow [X] | Reports a wall-clock value reaching a branch condition as DET005 at warning severity [V release-diff.patch:66] | bashrs provides advanced determinism analysis | reject(bashrs detects domain-specific hazards shellcheck ignores) | `release-diff.patch:66` | CHANGELOG.md |
| 95 | B11 | L2 | semgrep bash rules | Can theoretically flag date usage in if statements if rules are written [X] | Distinguishes timestamps reaching artifacts (DET002) from branch conditions (DET005) [V release-diff.patch:275] | bashrs provides precise built-in determinism semantics | reject(built-in data flow tracking is more accurate than regex rules) | `release-diff.patch:275` | book/src/linting/determinism.md |
| 96 | B11 | L2 | shellharden | Checks for proper quoting of command substitutions, not their semantics [X] | Flags relative-deadline timeout loops as DET005 warnings [V release-diff.patch:297] | bashrs analyzes shell script control flow dynamically | reject(bashrs covers dynamic shell script hazards) | `release-diff.patch:297` | book/src/linting/determinism.md |
| 97 | B11 | L3 | shellcheck | does not evaluate determinism or time-dependent conditions [X] | reports time-dependent control flow at warning as DET005, not DET002 [V release-diff.patch:66] | bashrs implements specific rules for non-deterministic execution paths | reject(separating artifact contamination from control flow non-determinism provides clearer diagnostics) | `release-diff.patch:66` | CHANGELOG.md |
| 98 | B11 | L3 | semgrep | flags sensitive API calls in conditions based on custom configuration [X] | reports time-dependent control flow at warning as DET005, not DET002 [V release-diff.patch:66] | bashrs categorizes the vulnerability natively as DET005 | reject(separating artifact contamination from control flow non-determinism provides clearer diagnostics) | `release-diff.patch:66` | CHANGELOG.md |
| 99 | B11 | L3 | shellharden | focuses strictly on quoting and ignores logical control flow bugs [X] | reports time-dependent control flow at warning as DET005, not DET002 [V release-diff.patch:66] | bashrs inspects both string safety and semantic determinism | reject(separating artifact contamination from control flow non-determinism provides clearer diagnostics) | `release-diff.patch:66` | CHANGELOG.md |
| 100 | B12 | L1 | shellcheck's own CI | maintains thousands of inline unit tests and parses some large real-world scripts [X] | Corpus: 225 entries in six themes [V release-diff.patch:67] | bashrs pulls directly from 164 sovereign repositories | reject(bashrs's sovereign corpus approach provides strong real-world coverage) | `release-diff.patch:67` | CHANGELOG.md |
| 101 | B12 | L1 | forjar's scripts/dogfood | tests against known recipes and fixtures, explicitly bounding regressions [V forjar:/home/noah/src/forjar/scripts/dogfood/docs.sh:41] | Corpus: 225 entries in six themes [V release-diff.patch:67] | bashrs uses 225 real scripts from the wild and a strict RELEASE_BAR count like forjar | reject(bashrs's corpus design is robust and data-driven) | `release-diff.patch:67` | CHANGELOG.md |
| 102 | B12 | L1 | pre-commit | validates hooks by running them over a massive set of repositories [X] | Corpus: 225 entries in six themes [V release-diff.patch:67] | bashrs sandboxes the corpus in bwrap and tracks regressions mechanically | reject(bashrs bwrap approach is safe and deterministic) | `release-diff.patch:67` | CHANGELOG.md |
| 103 | B12 | L2 | shellcheck's own CI | Tests against massive real-world corpora of bash scripts [X] | Uses 225 entries chosen by measured gaps across 164 sovereign repositories [V release-diff.patch:67] | bashrs targets specific unrepresented paradigms | reject(bashrs's gap-driven corpus selection is rigorous) | `release-diff.patch:67` | CHANGELOG.md |
| 104 | B12 | L2 | forjar's scripts/dogfood | Runs dogfood tests on internal scripts [V forjar:scripts/dogfood/docs.sh:10] | Corpus includes make metaprogramming, heredocs, agentic loops [V release-diff.patch:67] | bashrs covers a broader ecosystem than just internal scripts | reject(bashrs needs broad real-world testing) | `release-diff.patch:67` | CHANGELOG.md |
| 105 | B12 | L2 | mvdan/sh | Uses comprehensive bash script suites to test parser edge cases [X] | Excluded seven entries before run because they execute side effects [V release-diff.patch:67] | bashrs isolates behavioral checks from side effects | reject(bashrs safely handles side-effecting scripts) | `release-diff.patch:67` | CHANGELOG.md |
| 106 | B12 | L3 | shellcheck | accumulates a massive corpus of real-world bash scripts to test rule accuracy [X] | adds 225 entries chosen by measuring the gap against 2,890 scripts from 164 sovereign repositories [V release-diff.patch:67] | bashrs uses data-driven gap analysis rather than arbitrary accumulation | reject(gap-driven corpus selection maximizes marginal coverage) | `release-diff.patch:67` | CHANGELOG.md |
| 107 | B12 | L3 | mvdan/sh | maintains a test corpus focusing heavily on syntax edge cases and language constructs [X] | adds 225 entries chosen by measuring the gap against 2,890 scripts from 164 sovereign repositories [V release-diff.patch:67] | bashrs selects corpus entries based on real-world usage frequencies like systemd/cron | reject(gap-driven corpus selection maximizes marginal coverage) | `release-diff.patch:67` | CHANGELOG.md |
| 108 | B12 | L3 | tree-sitter-bash | uses community-contributed examples to test parsing boundaries [X] | adds 225 entries chosen by measuring the gap against 2,890 scripts from 164 sovereign repositories [V release-diff.patch:67] | bashrs builds a corpus via telemetry gap analysis rather than manual curation | reject(gap-driven corpus selection maximizes marginal coverage) | `release-diff.patch:67` | CHANGELOG.md |

### Systems each lane chose, per bullet

| Bullet | Lane 1 | Lane 2 | Lane 3 |
|--------|--------|--------|--------|
| B1 | Batsh, Amber, Oil/YSH (0 adopt) | POSIX shell quoting rules, Amber, Batsh (0 adopt) | Batsh, Amber, Oil/YSH (0 adopt) |
| B2 | POSIX shell quoting rules, mvdan/sh's printer, shellcheck SC2016 (0 adopt) | shellcheck SC2006/SC2016, mvdan/sh, Oil/YSH (0 adopt) | POSIX shell, shellcheck, shellharden (0 adopt) |
| B3 | shellcheck, mvdan/sh, tree-sitter-bash (0 adopt) | shellcheck, tree-sitter-bash, bash (0 adopt) | shellcheck, bash, mvdan/sh (0 adopt) |
| B4 | shellcheck, hadolint, trivy (0 adopt) | shellcheck, hadolint, semgrep (0 adopt) | shellcheck, hadolint, checkov (0 adopt) |
| B5 | cargo's own workspace behaviour, cargo-release, release-plz (0 adopt) | cargo's own workspace behaviour, cargo-release, release-plz (0 adopt) | cargo workspace, cargo-release, release-plz (0 adopt) |
| B6 | forjar's scripts/publish-from-tag.sh, cargo-release, release-plz (0 adopt) | forjar's scripts/publish-from-tag.sh, cargo-release, release-plz (0 adopt) | forjar, release-plz, cargo workspace (0 adopt) |
| B7 | shellcheck, semgrep bash rules, shellharden (0 adopt) | shellcheck, semgrep bash rules, shfmt (0 adopt) | shellcheck, semgrep, shellharden (0 adopt) |
| B8 | shellcheck's security checks, semgrep bash rules, hadolint (0 adopt) | shellcheck, GNU make, bash (0 adopt) | shellcheck, semgrep, hadolint (0 adopt) |
| B9 | shellcheck, mvdan/sh, tree-sitter-bash (0 adopt) | shellcheck, mvdan/sh, tree-sitter-bash (0 adopt) | shellcheck, mvdan/sh, tree-sitter-bash (0 adopt) |
| B10 | forjar's scripts/dogfood, pre-commit, shellcheck's own CI (0 adopt) | forjar's scripts/dogfood, pre-commit, shellcheck's own CI (0 adopt) | forjar, pre-commit, shellcheck (0 adopt) |
| B11 | shellcheck's security checks, semgrep bash rules, shellharden (0 adopt) | shellcheck, semgrep bash rules, shellharden (0 adopt) | shellcheck, semgrep, shellharden (0 adopt) |
| B12 | shellcheck's own CI, forjar's scripts/dogfood, pre-commit (0 adopt) | shellcheck's own CI, forjar's scripts/dogfood, mvdan/sh (0 adopt) | shellcheck, mvdan/sh, tree-sitter-bash (0 adopt) |

### Dissent

Every `[DISSENT]` finding, verbatim, by lane. All three lanes returned PASS, so lane-reduce `dissent[]` is empty (it holds only non-PASS lanes); the panel dissent lives in these rows.

- **Lane 1**: [DISSENT B3] system=mvdan/sh \| how_they_do_it=constructs a full AST where loop containment is absolute [X] \| bashrs_today=processes loop-start keywords, done and break/continue in the byte order they occur on a physical line [V release-diff.patch:1521] \| delta=AST parsing perfectly resolves all edge cases, while byte-order scanning is fragile — disposition `adopt(AST parsing for SC2105)`; cite `release-diff.patch:1521` resolves to rash/src/linter/rules/sc2105.rs.
- **Lane 2**: [DISSENT B6] system=cargo-release \| how_they_do_it=Cargo-release handles topology and registry indexing natively in Rust [X] \| bashrs_today=Uses a complex bash script with jq and curl polling [V release-diff.patch:117] \| delta=bashrs scripts are harder to maintain than native Rust tools — disposition `adopt(cargo-release for automated workspace publishing)`; cite `release-diff.patch:117` resolves to Makefile.
- **Lane 3**: [DISSENT B6] release-plz automates topological publishing without manual scripts, which is the stronger argument for most Rust projects, but bashrs's choice stands because it enforces strict detached worktree isolation. — disposition `none`; cite `release-diff.patch:10` resolves to .github/workflows/release.yml.
- **Lane 3**: [DISSENT B1] Amber and Batsh use native bash arrays when the target shell supports it. Amber has the stronger argument for safety and performance, but bashrs is constrained by targeting POSIX sh. — disposition `none`; cite `release-diff.patch:50` resolves to CHANGELOG.md.

## Provenance

### Where the [V] citations resolve

| Lane | code, build or CI | CHANGELOG.md (the bullet text itself) | docs, book or contracts | unresolved |
|------|------|------|------|------|
| L1 | 32 | 5 | 0 | 0 |
| L2 | 7 | 22 | 8 | 0 |
| L3 | 7 | 31 | 0 | 0 |

### What each lane opened (from `brain/<conversation>/.system_generated/logs/transcript_full.jsonl`)

- **L1** (`f0b5390b-cf07-4d4a-a574-fe9293156b89`): view_file release-diff.patch:690-790 release-diff.patch:3426-3514 forjar/scripts/publish-from-tag.sh:1-end forjar/scripts/dogfood/docs.sh:1-50 ; grep_search `GH-293;GH-294;PMAT-244|SC2105;GH-301|PMAT-253|crates\.io|bashrs disable-|shell_words|dogfood-selflint;panic = "abort";BASHRS001;duration;225;`; run_command 1 (steps whose output is a fork/exec failure: 1)
- **L2** (`dc7ede87-235d-451f-a4ef-42e7023d24d8`): view_file release-diff.patch:1-800 forjar/scripts/publish-from-tag.sh:1-end forjar/scripts/dogfood/docs.sh:1-20 ; grep_search `array_join;GH-294;PMAT-244;gh301;`; run_command 2 (steps whose output is a fork/exec failure: 2)
- **L3** (`215174d3-5dcc-46d0-91c7-e25b39e6796c`): view_file release-diff.patch:1-200 release-diff.patch:3156-3735 forjar/scripts/publish-from-tag.sh:1-end ; grep_search ``; run_command 0 (steps whose output is a fork/exec failure: 0)
