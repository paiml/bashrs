# Allow useless comparisons in test code (usize >= 0)
# These are defensive assertions that don't harm anything
# TODO: Clean up these assertions properly in next release

# Disallowed methods — prevent Cloudflare-class defects
disallowed-methods = [
    { path = "std::option::Option::unwrap", reason = "Use expect() with descriptive message instead — unwrap() causes panics (Cloudflare 2025-11-18)" },
    { path = "std::result::Result::unwrap", reason = "Use expect() with descriptive message instead — unwrap() causes panics (Cloudflare 2025-11-18)" },
]
